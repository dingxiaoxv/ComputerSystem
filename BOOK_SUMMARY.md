# CSAPP 全书总结

这份文档取代书里各章的小结小节（§2.5、§3.12、§5.15、§6.7、§8.8、§9.12、§12.8），把十一章（第 4 章按计划跳过）的主干和最值钱的易错点收在一处。每章只留三样东西：**这一章到底在解决什么问题**、**读完必须记住的骨干链条**、**最容易在真实工程里踩到的坑**。细节回到各章 `summary.md`，这里只做索引和串讲。

---

## 全书主线：一个程序的一生

```
        源码 hello.c
           │  ① 预处理/编译/汇编          ← 第 2、3 章：数据怎么编码，代码怎么变成指令
           ▼
        hello.o （可重定位目标文件）
           │  ② 静态链接：符号解析 + 重定位  ← 第 7 章
           ▼
        hello   （可执行目标文件，ELF）
           │  ③ fork + execve             ← 第 8 章：谁来启动它
           ▼
        进程（私有地址空间 + 逻辑控制流）   ← 第 8、9 章：两个核心假象
           │  ④ 缺页 → demand paging      ← 第 9 章：代码数据是被"用到才装进来"的
           ▼
        指令在流水线上执行                 ← 第 5 章：延迟界限 / 吞吐量界限
           │  ⑤ 每次访存穿过 L1/L2/L3/DRAM ← 第 6 章：局部性决定实际速度
           ▼
        read/write 越过内核边界            ← 第 10 章：一切皆文件
           │  ⑥ socket 把边界拉到另一台机器 ← 第 11 章
           ▼
        多个执行流同时干这件事              ← 第 12 章：并发与同步
```

**三个贯穿全书的假象**，每一个都由一层机制维持：

| 假象 | 谁提供 | 代价 / 漏出来的地方 |
|------|--------|---------------------|
| 我独占 CPU | 内核的上下文切换（第 8 章） | 上下文切换开销、竞态、信号打断 |
| 我独占一大块连续内存 | 虚拟内存 + MMU（第 9 章） | 缺页、TLB miss、段错误、COW |
| 内存访问都一样快 | 存储器层次结构（第 6 章） | cache miss 让相同算法差几十倍 |

**贯穿全书的一句话**：几乎所有"性能/正确性玄学"，都是某个抽象的实现细节漏了出来。

---

## 第 1 章 计算机系统漫游

**🎯 主线**：先给全书画地图——信息就是位加上下文；程序要经过预处理、编译、汇编、链接四步才能跑；硬件用存储层次弥合速度差；OS 用进程、虚拟内存、文件三个抽象隔离硬件；Amdahl 定律给所有优化定了天花板。

**🎯 骨干**

- 同一串字节是 `int` 还是 `float` 还是指令，取决于**读它的上下文**，不取决于数据本身。
- OS 提供的三大抽象一一对应后面三章：进程（第 8 章）、虚拟内存（第 9 章）、文件（第 10 章）。
- Amdahl 定律 $S = \frac{1}{(1-\alpha) + \alpha/k}$：只优化占比 $\alpha$ 的部分，天花板是 $1/(1-\alpha)$。

**⚠️ 易错点**

- "把这段代码优化 100 倍"从来不等于程序快 100 倍——先测 $\alpha$，再决定值不值得动手（第 5 章 dedup 实验实测 7.30× vs 预测 7.36×，误差 <1%）。

---

## 第 2 章 信息的表示和处理

**🎯 主线**：机器里只有有限位宽的位模式。整数运算是模运算，浮点运算是带舍入的近似——一切"诡异的 bug"都来自把它们当成数学上的整数和实数。

**🎯 骨干**

- 补码是唯一编码：$B2T_w(\vec{x}) = -x_{w-1}2^{w-1} + \sum_{i=0}^{w-2} x_i 2^i$，范围不对称（`INT_MIN` 没有对应的正数）。
- 有符号与无符号混合运算，**有符号一律转无符号**——`-1 < 0U` 为 `false`。
- 扩展是符号扩展还是零扩展，由**源类型**决定；截断直接丢高位，符号位跟着丢。
- IEEE 754：`V = (-1)^s × M × 2^E`，规格化/非规格化/特殊值三档由阶码字段区分。
- 浮点加法**不满足结合律**，整数加乘满足（模 $2^w$ 一致）——这决定了编译器敢对整数做重结合、不敢对浮点做。

**⚠️ 必记易错点**

- `size_t`/`unsigned` 做倒序循环计数器时 `i >= 0` 恒真，是死循环的经典来源。
- **有符号溢出是 UB**（编译器会直接删掉 `x + 1 > x` 这种"溢出检查"），**无符号溢出是合法模运算**——两者待遇完全不同。
- `n * sizeof(T)` 会溢出：`calloc` 内部查，`malloc` 不查，这是经典堆溢出漏洞模式。
- 负数右移不是除法，差一个偏置；汇编里 `sar + and + add` 序列就是在做这个修正。
- `NaN != NaN`，检测必须 `isnan()`；`float` 精确表示整数的上限是 $2^{24}$。
- `-ffast-math` 打破 IEEE 754 严格语义，允许重排浮点运算——精度敏感代码不能开。

**🔧 工程落点**：跨平台一律用 `<cstdint>`（`long` 在 Linux 是 8 字节、Windows 是 4 字节）；字节序只在调试、网络、文件三种跨边界场景暴露。

---

## 第 3 章 程序的机器级表示

**🎯 主线**：把 C 的每一种构造（变量、条件、循环、函数、数组、结构体、指针）翻译成 x86-64 的寄存器与指令，从此能用 `objdump`/`gdb` 反推程序的真实行为。

**🎯 骨干链条**

| C 构造 | 机器级实现 | 关键点 |
|--------|-----------|--------|
| 局部变量 | 寄存器优先，放不下才进栈 | 叶子函数可能完全不碰 `%rsp` |
| `a + b*8 + 4` | `leaq` | `leaq` **不访存**，是编译器的通用算术工具 |
| `if/else` | `jmp` 或 `cmov` | cmov 两条路都算，有副作用/可能段错误就退回分支 |
| `for/while` | do-while 骨架 + 跳到中间 | 判断在底部，减少一次跳转 |
| `switch` | 跳转表 `jmp *(%rax,...,8)` | 越界检查用 `ja`，一条指令同时排除负数和超界 |
| 函数调用 | `call` 压返回地址 + 6 个参数寄存器 | `%rdi,%rsi,%rdx,%rcx,%r8,%r9`，第 7 个起逆序压栈 |
| 数组 `A[i][j]` | `base + (i*C + j)*size` | 行优先，内层循环走最右下标才是 stride-1 |
| 结构体 | 字段顺序 + padding | 编译器**不会**帮你重排字段 |

**⚠️ 必记易错点**

- AT&T 语法源在左目的在右：`subq %rax, %rdx` 是 `rdx -= rax`；`cmp a, b` 算的是 `b - a`。
- 写 `%eax` 会自动清零 `%rax` 高 32 位，写 `%al`/`%ax` 不会。
- 判断一条指令碰不碰内存看操作数有没有 `(...)`，不看助记符；三个例外记死：`push/pop/call/ret` 一定碰栈、`lea` 一定不碰。
- **caller-saved / callee-saved 说的是"谁有保存义务"**，不是"谁在用"，极易记反。
- 缓冲区溢出真正的危险不是覆盖变量，而是覆盖**返回地址**劫持控制流；canary 只在函数返回时检查，栈帧内部越界发现不了；NX 挡不住 ROP。
- `sizeof(struct)` ≠ 字段大小之和；末尾 padding 的目的是让**数组中每个元素**都保持对齐。
- `%rbp` 不是必需的帧指针，`-O1` 起 GCC 默认省略，只有变长栈帧才强制需要。

**🔧 工程落点**：字段先大后小通常最紧凑；`alignas(64)` 隔开热点字段防 false sharing（第 6/12 章会再遇到）；packed 结构取未对齐字段指针是 UB，在 ARM 上直接崩。

**🎯 浮点是并行的另一套世界（§3.11）**

- 独立的 `%xmm0-15`，和整数寄存器毫无重叠；**靠指令后缀而非寄存器名区分宽度**：`ss`(1×float)、`sd`(1×double)、`ps`/`pd`(打包多个)。
- 参数走 `%xmm0-7`，**和整型参数各排各的队**；返回值在 `%xmm0`。
- **全部 16 个 `%xmm` 都是 caller-saved**——浮点值跨函数调用只能溢出到栈，整数版则可以待在 `%rbx` 里。
- 符号操作是位运算：取负 `xorpd` 符号位掩码，`fabs` 用 `andpd`。但手写 `x<0?-x:x` **不等价**（对 `-0.0` 行为不同），编译器不敢替你优化。
- 浮点比较多出「无序」状态：`comisd`/`ucomisd` 把结果编码进 CF/ZF/**PF**，所以要用 `ja`/`jb` 无符号跳转，判等必须同时检查 PF 和 ZF，否则 `NaN == NaN` 会为真。
- 浮点常数没有立即数形式，一律从 `.rodata` 加载（只有 `0.0` 能用 `pxor` 自异或省掉）。

---

## 第 4 章 处理器体系结构

按学习计划**跳过，不记录笔记**。需要的执行模型（超标量、乱序、流水线、投机执行）由 §5.7 从性能视角补齐，够用。

---

## 第 5 章 优化程序性能

**🎯 主线**：性能不是"多写几个技巧"，而是**先量、再定位、最后按层次施力**。编译器受 safe optimization 约束不敢做的事，才轮到人上手。

**🎯 骨干：两条界限决定一切**

```
延迟界限 (latency bound)   = 关键路径上运算延迟之和 / 迭代
    ↑ 单累加器写法卡在这里，循环展开救不了
吞吐量界限 (throughput bound) = issue_time / capacity
    ↑ 处理器的物理极限，靠打断依赖链（多累加器/重结合）才能逼近
```

**🎯 优化的正确顺序（不可颠倒）**

1. **算法与数据结构**（O(n²) → O(n)，收益最大，本章 dedup 实验单点 187×）
2. **消除妨碍编译器的因素**：把不变量提出循环、消除内存引用改用寄存器累加、必要时 `restrict`
3. **打断依赖链**：多累加器、重结合（累加器数量饱和点 = `latency × capacity`，超了会寄存器溢出反变慢）
4. **最后才考虑**：分支消除、cmov、手工向量化

**⚠️ 必记易错点**

- **编译器不敢优化的头号原因是内存别名**（`*dest` 可能指向 `data` 内部），不是编译器笨；其次是函数调用可能有副作用。
- 循环展开 ≠ 提高并行性：它只摊薄循环开销，**打不断依赖链**，对延迟受限的浮点乘法 CPE 完全不变。
- `for (i=0; i<strlen(s); i++)` 是隐藏的 O(n²)——code motion 的头号现实陷阱。
- issue time = 1 不代表 1 周期算完；延迟仍可能是 5 周期（完全流水线化）。
- 除法慢的真凶是**不流水线化**（capacity=1、issue≈latency），多累加器救不回来。
- 不要无差别消除分支：循环边界、错误检查几乎不会预测错；值钱的目标是**数据相关、模式随机**的分支。cmov 只在分支难预测时才赢，可预测数据下反而更慢。
- store 不是天然比 load 快：一旦后续 load 撞上刚写的地址（别名），store→load 转发把延迟拉到 ~6 cycle。
- 链表遍历 CPE≈4 是 **load 延迟串成关键路径**，不是 cache 慢——命中 L1 也救不了。

**🔧 工程落点：profiling 工作流**

```bash
perf stat -e cycles,instructions,cache-misses,branch-misses ./prog   # 先看全局特征
perf record -g ./prog && perf report                                 # 找热点
perf annotate                                                        # 精确到指令行
```

- **gprof 会翻车**：本项目实测它把 `strcmp` 的 63% 错归给 `_init`（self time 不含被调库函数）；`perf` 采 PC 正确钉到 `__strcmp_avx2` 65%。
- 精确指令计数用 `callgrind`，采样定位用 `perf`，两者用途不同。

---

## 第 6 章 存储器层次结构

**🎯 主线**：CPU 与 DRAM 的速度差有两三个数量级，靠**局部性 + 层次化缓存**弥合。程序员改不了硬件，只能改访问模式。

**🎯 骨干**

- cache 组织：`C = S × E × B`，地址切成 `[标记 t | 组索引 s | 块偏移 b]`。**组索引取地址中间位**——高位索引会把顺序访问全挤进一个组。
- 三种 miss：冷 miss（与容量无关）、冲突 miss（**cache 没满照样发生**）、容量 miss（加大 cache 才能解）。
- 写策略是**两个正交问题**：写命中（写直达 / 写回）× 写不命中（写分配 / 非写分配）。现代 cache 通常是写回 + 写分配。
- 缓存友好的核心只有一句：**让内层循环走 stride-1**，C 数组行优先，内层循环必须对应最右下标。
- 本机实测延迟阶梯（随机指针追逐，绑核）：**L1≈4、L2≈14、L3≈40、DRAM≈300+ cycle**。

**⚠️ 必记易错点**

- 缓存友好 ≠ 减少访存：行优先和列优先**访存次数完全一样**，差别只在"这些访存能不能命中"。
- **顺序访问测不出缓存效应**：硬件预取器会提前把数据搬进 L1，`L1-dcache-load-misses` 恒定在冷启动量级，工作集从 32KB 涨到 64MB miss 率不升反降。要暴露延迟阶梯必须用**随机访问/指针追逐**打败预取器。
- **做 cache 实验前必须先写一遍数组**：未初始化的 `.bss` 大数组所有页都映射到内核共享零页，64MB 虚拟数组背后只有 4KB 物理页，测的根本不是访问模式（本项目在此翻车过一次，见第 9 章零页别名）。
- stride 的伤害到"每条 line 只剩 1 个元素"就到顶（本机 stride=8），再大不会无限恶化。
- 存储器山有**两个独立维度**：工作集大小（在哪级存储）和步长（空间局部性）；工作集装进 L1 时 stride 几乎不影响性能。
- false sharing 不是数据竞争（结果是对的），是纯性能问题——共享的不是变量而是同一条 cache line，`alignas(64)` 隔开即可。
- 混合架构（P-core + E-core）上 `perf stat -e L1-dcache-load-misses` 会三行全空，必须 `perf stat -e cpu_core/.../ taskset -c 0 ./prog` 显式选 P-core 事件并绑核。

**🔧 工程落点**：矩阵乘 6 个循环顺序版本**计算量完全相同**，性能差几倍纯来自 miss 率；分块（blocking）、结构体拆分（SoA vs AoS）、`alignas` 隔离热点是三个最常用的手段。

---

## 第 7 章 链接

**🎯 主线**：分离编译需要有人把散落的符号引用接起来——链接器做两件事：**符号解析**（每个引用找到唯一定义）和**重定位**（把占位地址改成真地址）。

**🎯 骨干**

```
.c ──gcc -c──> .o (可重定位)  ──ld──> a.out (可执行) ──execve──> 进程
                  │                        │
              .symtab                   程序头表（段：只读代码段 / 读写数据段）
              重定位条目               ──dlopen/ld.so──> .so (共享库)
```

- 符号三类：全局定义、外部引用（`U`）、本地 `static`。
- **强弱符号规则**：函数和已初始化全局变量是强符号，未初始化全局变量是弱符号；两个强符号报错，强弱共存选强的，多个弱符号任选一个——这是最隐蔽的 bug 来源。
- 重定位类型：`R_X86_64_PC32`（相对偏移，要 `-4` 补偿到下一条指令）、`R_X86_64_64`（绝对地址）。
- PIC 通过 **GOT**（数据间接寻址）和 **PLT**（函数延迟绑定）实现地址无关。

**⚠️ 必记易错点**

- **链接命令行顺序敏感**：引用方在前、库在后，否则 undefined reference。这是链接阶段规则，和编译时文件顺序无关。
- 符号表里的 "LOCAL" 指 `static` 符号，**不是函数里的局部变量**——普通局部变量在栈上，根本不进符号表。
- `.bss` 不占磁盘空间，只记大小——大数组放全局不会撑大可执行文件。
- 静态库 `.a` 只抽取用到的 `.o`；`.so` 是不可分割的整体，没人用的函数也会被一起映射。动态链接省的是**物理内存的总份数**，不是单个文件更小。
- 引用 `.so` 的全局变量在可执行文件符号表里显示为已定义（`B`/`D`，因为 copy relocation 搬进了 `.bss`），只有函数才是 `U`。
- `strip` 后 `nm` 报 "no symbols"，但 `nm -D`（看 `.dynsym`）仍有输出。
- 别对不可信文件直接 `ldd`，用 `readelf -d` 看 `NEEDED` 更安全。

**🔧 工程落点**：`LD_PRELOAD` 打桩能在不改不重编的前提下替换库函数（malloc 统计、故障注入），也是安全风险来源；现代 GCC 默认 `-fno-common` 把弱符号冲突从静默共用内存变成报错。

---

## 第 8 章 异常控制流

**🎯 主线**：程序不是一条直线跑到底。从硬件中断到进程、信号、非局部跳转，ECF 是**每一层"突变控制流"机制的统称**，也是 OS 得以存在的基础。

**🎯 骨干：四类异常**

| 类别 | 起因 | 异步? | 返回到 |
|------|------|-------|--------|
| 中断 | 外部设备信号 | **异步** | 下一条指令 |
| 陷阱 | 有意的指令（`syscall`） | 同步 | 下一条指令 |
| 故障 | 潜在可恢复的错误（缺页） | 同步 | **当前指令**（重试）或终止 |
| 终止 | 不可恢复的硬件错误 | 同步 | 不返回 |

**🎯 进程控制的四个动作**

- `fork`：**一次调用两次返回**，父得 PID、子得 0；地址空间 COW 复制。
- `execve`：**调用一次永不返回**，它后面的代码只有失败时才执行。
- `waitpid`：回收僵尸；`SIGCHLD` **不排队**，必须 `while (waitpid(-1, &s, WNOHANG) > 0)`。
- 信号：pending 是**位向量不是计数器**，待处理期间多来的同种信号会丢。

**⚠️ 必记易错点**

- 异常 ≠ 程序出错：每次 `read`/`write` 都是陷阱，属于**正常控制流**。
- fork 后父子执行顺序**完全不确定**；变量是独立副本，只是初值相同。
- 僵尸进程占的是 PID 和进程表项，不是内存；长期不回收会耗尽 PID。
- `cd` 必须内置，不能 `fork+execve`——子进程 `chdir` 改的是子进程的目录。
- 系统调用**返回 -1 才看 `errno`**，成功时 `errno` 的值未定义。
- 信号处理程序里绝不能用 `printf`/`malloc`（非异步信号安全），要用基于 `write` 的 `sio_*`；进入存 `errno`、退出还原；全局标志必须 `volatile sig_atomic_t`。
- `SIGKILL`/`SIGSTOP` 无法捕获、忽略或阻塞。
- 阻塞信号 ≠ 丢弃，解除阻塞瞬间会投递，但合并后只剩一个。
- 从 handler 跳回主程序必须用 `sigsetjmp`/`siglongjmp`（`savesigs` 传非 0），普通 `longjmp` 会丢信号掩码；`longjmp` 只能跳进**尚未返回**的函数。
- `D` 状态（不可中断睡眠）连 `kill -9` 都得等它从内核等待点醒来；`D` 只是不响应信号唤醒，硬件中断照常发生。

**🔧 工程落点**：`strace` 追多线程/多进程服务**必须加 `-f`**，否则只看主线程会误判"没在做事"；`ps` 是快照适合脚本、`top` 是周期刷新适合实时；`load average` 不是 CPU 百分比，要和核数比；看可用内存要 `MemAvailable` 不是 `MemFree`。

---

## 第 9 章 虚拟内存

**🎯 主线**：VM 首先是**地址空间抽象**（每次访存都在翻译），其次才是"用 DRAM 缓存磁盘"。它同时解决了三件事：内存管理（简化链接加载共享）、内存保护（权限位）、缓存（demand paging）。

**🎯 骨干：一次访存的完整旅程**

```
虚拟地址 VA = [VPN | VPO]
   │
   ├─ TLB 命中 ────────────────────► PPN（零次内存访问，全在芯片内）
   │
   └─ TLB miss → 查多级页表（x86-64 四级基数树）
                    │
                    ├─ PTE 有效 → PPN，回填 TLB
                    └─ PTE 无效 → 缺页异常
                                    │
                                    ├─ 地址不在任何 vm_area_struct → SIGSEGV
                                    ├─ 违反区域权限            → SIGSEGV
                                    └─ 合法 → 换入/分配物理页 → 重新执行该指令
PPN | PPO = 物理地址 ──► L1/L2/L3 cache ──► DRAM
```

**🎯 三个必须分清的概念对**

| 概念对 | 区别 |
|--------|------|
| minor fault / major fault | 前者只修 PTE 不碰磁盘，后者要等磁盘 I/O |
| page cache / CPU cache | 前者内核软件管理、4KB 页、缓存文件；后者硬件、64B 行、缓存 DRAM。**串联两级** |
| 共享对象 / 私有对象 | `MAP_SHARED` 写回文件；`MAP_PRIVATE` 第一次写触发 COW，改不到磁盘 |

**⚠️ 必记易错点**

- `malloc` 成功只是登记虚拟页映射，**物理页要等第一次触摸才分配**——大块 malloc 秒回且 RSS 不涨。
- `free` 后 RSS 通常**不降**：分配器留着空闲块复用，不还给内核，这是设计如此不是泄漏。
- **fork 不复制内存**，只复制页表并把私有页标只读；`execve` 不把程序读进内存，只建映射——所以大程序启动很快。
- 段错误和正常缺页走**同一个入口**，区别只在出口的两道关卡。
- **只读不写的 `.bss` 大数组所有页都映射到内核共享零页**，物理内存几乎为零——做内存/cache benchmark 必须先写入初始化（本项目最贵的一次翻车）。
- 多级页表省的是"未映射区域的下级页表"，不是让翻译变快；**查得快靠 TLB**。
- CSAPP 的单级页表是教学简化：x86-64 单级页表每进程要 512 GB（$2^{48}/2^{12}×8$B）。
- 脏页的"脏"是相对后备存储而言；匿名脏页只能写回 swap，无 swap 时不可驱逐，内存紧张只能 OOM。
- 脏页最多躺 30 秒必落盘（flusher 5 秒醒 + 30 秒过期 + `dirty_ratio` 阈值），别以为"不缺页就不刷盘"。
- 统计多进程内存时把每个进程 maps 里的 libc 直接相加会严重高估——要看 **PSS**（按共享份数均摊）而非 RSS。
- 解析 `/proc/<pid>/stat` 不能按空格 split，第 2 字段 `(comm)` 可能含空格，要从**最后一个 `)`** 之后数。

**🎯 动态内存分配（§9.9-9.10）**

- 分配器的两个目标（吞吐率 vs 利用率）是一对死敌；碎片分内部（由过去请求决定、可度量）和外部（由未来请求决定、不可预测）。
- 头部能藏 alloc 标志，是因为块按 8/16 对齐使 size 低位恒为 0。
- **边界标记（脚部）专门为前向合并 O(1) 而设**。
- 主流分配器走**分离空闲链表**（按大小分桶），不是隐式链表。
- GC 的"垃圾"是**从根不可达的块**，不是"逻辑上不再需要"；引用计数收不了环，必须 `weak_ptr` 打断。

**🔧 工程落点**：`/proc/<pid>/smaps_rollup` 一眼看清进程内存构成；`/proc/meminfo` 看系统全景；排查"内存高"先分清是 page cache（文件读写）还是匿名页（堆申请）；iowait 高时看 `Dirty`/`Writeback` 定位回写风暴。本项目另有 [内核虚拟内存说明书](Chapter9/kernel-vm-source/00-index.md)（ARM64 + 6.x 内核源码级）和 [zswap 优化方案](Chapter9/zswap.md)。

**🎯 常见内存错误（§9.11）：最危险的是「不崩」**

实测十类内存错误，**四类在普通编译下退出码为 0**（读未初始化、堆 off-by-one、界内指针错位、内存泄漏）；会崩的那几类，**崩溃点也不在错误点**——堆越界破坏的是分配器元数据，要等下一次 `malloc`/`free` 才暴露，调用栈指向的是受害者不是凶手。

工具能力边界（本机实测，三者互补且都有盲区）：

| | 抓不到什么 |
|---|---|
| **ASan** | **读未初始化内存**（完全静默） |
| **Valgrind** | **栈缓冲区溢出**（只有 canary 兜住） |
| **两者都** | **界内的指针错位**——地址合法，只是位置错了 |

`malloc` 不清零、`calloc` 才清零；`free` 只接受返回的原地址，`free(p); p = NULL;` 能把 double free 变成无害操作。

---

## 第 10 章 系统级 I/O

**🎯 主线**：Unix 把所有 I/O 设备都模型化为文件，用 `open/read/write/close` 四个调用统一。理解**打开文件表**这层间接，才能解释 fork 共享偏移量、重定向、以及 `dup2` 为什么能工作。

**🎯 骨干：三张表**

```
进程 A                    内核
描述符表          打开文件表              v-node 表
 fd 0 ──┐        ┌─► 文件位置 k ──┐     ┌─► 文件元数据
 fd 1 ──┼───────►│   引用计数     ├────►│   （stat 里的信息）
 fd 4 ──┘        └────────────────┘     └───────────────
```

- **文件偏移量 `k` 存在打开文件表项里**，不在 fd、也不在 v-node 里。两次 `open` 同一文件有各自独立的 `k`；`fork`/`dup2` 共享同一个 `k`。
- 重定向 `dup2(fd, 1)` 就是让描述符表的槽 1 指向另一个打开文件表项。

**⚠️ 必记易错点**

- **short count 不是错误**：`read` 返回值比请求的 `n` 小是正常的，socket/管道/终端上尤其常见，必须循环（或用 RIO）。
- 返回值必须存 `ssize_t`，存进 `int` 或无符号类型会丢掉 -1 的错误语义。
- **EOF 不是字符**，没有"EOF 字节"，它是 `read` 返回 0 这一事件。
- 带缓冲的 `rio_readlineb` 和裸 `read` **不能混用同一个 fd**——缓冲里预读的数据会被裸 `read` 跳过。
- `rio_t` **不是线程安全的**，CSAPP 的前提是"一连接一 `rio_t`"，靠不共享回避锁。
- `fork` 后父子共享打开文件表项、引用计数为 2，**双方都要各自 `close`** 才真正释放。
- `readdir` 返回 `NULL` 要靠 `errno` 区分"读完"和"出错"。
- **二进制文件必须按字节数读**（`fread`/`read`/`ifstream.read`），不能用按分隔符切的文本函数——`0x0a`/`0x00` 在二进制里只是普通数据。

**🔧 工程落点**：选择准则是"能用标准 I/O 就用（磁盘文件、终端），网络 socket 必须用 RIO 或自己处理 short count"；跨机器的二进制格式交给 Protobuf/FlatBuffers，别手写（字节序 + 结构体 padding 两个坑）。另有 [UDS 本机 IPC 专题](Chapter10/uds_ipc.md)（SCM_RIGHTS 传 fd、抽象命名空间）。

---

## 第 11 章 网络编程

**🎯 主线**：网络编程 = 客户端-服务器模型 + socket 接口 + 应用层协议。socket 只是"跨机器的文件描述符"，前 10 章的知识（fork、dup2、execve、RIO）在 Tiny Web 服务器里全部复用。

**🎯 骨干：socket 接口动作图**

```
服务端                                客户端
socket()                              socket()
bind()      ← 绑定地址                getaddrinfo() ← 域名/服务名 → sockaddr
listen()    ← 转为监听描述符
accept() ◄──────── 连接请求 ───────── connect()
   │  返回新的 connfd
   ▼
rio_readlineb / rio_writen  ◄──────►  rio_readlineb / rio_writen
close(connfd)                         close(clientfd)
```

**⚠️ 必记易错点**

- **`accept` 返回的不是 `listenfd`**：它新建一个 `connfd`，混用会导致"只能服务一个客户端"。
- **TCP 是字节流没有消息边界**：一次 `read` 可能收到半条、也可能一条半（粘包），必须应用层自己切分（定长 / 长度前缀 / 分隔符）。
- IP 和端口在 socket 结构里是**大端**，必须 `ntohs`/`ntohl` 或让 `getnameinfo` 代劳。
- `getaddrinfo` 返回的链表必须 `freeaddrinfo`。
- 服务端不设 `SO_REUSEADDR`，重启时会被 `TIME_WAIT` 卡住 `bind`。
- **backlog 不是最大并发连接数**，只是等待 accept 的队列长度，且被 `somaxconn` 截断。
- **`dig` 能解析不代表程序能解析**：`dig` 只查 DNS，程序还要过 `/etc/hosts` 和 `nsswitch.conf`——排障用 `getent hosts` 才贴近真实行为。
- 把改状态的操作放进 `GET` 会被缓存/预取/爬虫误触发。

**🔧 工程落点**：TCP 三次握手的本质是交换并确认双方 ISN，同时协商 MSS/SACK/timestamp/window scale（本项目 tcpdump 实测）；DNS 走 UDP 是因为一问一答、重试成本低于建连成本。另有 [TCP/UDP 与 tcpdump 排障](Chapter11/tcp_udp.md)、[libcurl C++ 客户端](Chapter11/libcurl_cpp.md) 两个专题。

---

## 第 12 章 并发编程

**🎯 主线**：三种并发构造方式（进程 / I/O 多路复用 / 线程）各有边界；一旦共享可变状态，正确性问题就从"写对逻辑"升级为"建立协议"。

**🎯 骨干：三种模型的取舍**

| 模型 | 隔离性 | 共享成本 | 适用 |
|------|--------|----------|------|
| 多进程 | 地址空间隔离，最安全 | 必须 IPC（UDS/mmap/eventfd） | 需要故障隔离、特权分离 |
| I/O 多路复用 | 单线程，无锁 | 天然共享 | 海量低活跃连接，handler 不能阻塞 |
| 多线程 | 共享地址空间 | 最低，但需同步 | 需要利用多核 + 共享数据 |

**🎯 事件循环的关键认知**

- I/O 多路复用只通知 **readiness**，数据仍要由用户态 `accept/read/write` 处理（这是 Reactor 与 Proactor 的分界）。
- `select`（位图，受 `FD_SETSIZE` 限制、每轮原地改写）→ `poll`（数组，分离 `events`/`revents`）→ `epoll`（内核长期保存 interest set，只返回 ready）。
- **nonblocking ET 必须循环到 `EAGAIN`**，否则会漏数据/漏连接且不再收到通知。
- `EPOLLOUT` 只能在输出缓冲非空时按需开启，常开会 busy loop。
- 输出缓冲和任务队列必须设高水位和背压，否则慢客户端能把内存吃光。

**🎯 同步正确性的五要素**（比"哪里加锁"更根本）

> identity（哪个变量实例被共享）→ ownership（谁有权改）→ lifetime（对象活到什么时候）→ invariant（哪些字段必须一起改）→ ordering（happens-before 怎么建立）

**⚠️ 必记易错点**

- **没有 data race ≠ 没有竞态**：多个原子操作组成的 check-then-act 照样破坏业务不变量。
- `volatile` 不提供原子性也不提供 happens-before，不是线程安全工具。
- 临界区必须覆盖**完整的 read-check-modify 序列**，只给最终写加锁没用。
- condition variable 的 notify **不可积累**，持久状态必须放进受 mutex 保护的谓词，且必须 `while` 而非 `if`（spurious wakeup）。
- 先持 buffer mutex 再等 slots/items 会死锁——等待者占住了对方推进所需的锁。
- 线程栈是**逻辑私有而非硬件隔离**，地址逃逸出去就是共享变量。
- 向多个线程传同一个循环变量地址 `&i` 是经典错误，必须每线程独立参数槽。
- 线程共享 fd table，所以 thread-per-connection 里 main **不能**照搬 fork 版提前 `close(connfd)`。
- joinable 线程既不 join 也不 detach 会持续泄漏资源；detach 后不能再 join。
- `main` 返回等价于 `exit`，会终止整个进程和所有线程。
- **one loop per thread ≠ one connection per thread**：一个 EventLoop 用 epoll 管很多连接，一条连接固定归属一个 loop。
- 跨线程投递任务只保存 fd 是错的——fd 会复用，必须用稳定身份 + `weak_ptr` 校验生命周期。
- 线程安全 ≠ 可重入：内部加锁能串行化多线程调用，但持锁期间再次进入可能自锁；`recursive_mutex` 也不保证 callback 重入时状态一致。
- 给"返回静态指针"的函数加锁后原样返回指针没用——解锁后下次调用仍会覆盖。
- 线程数越多越快是错的：超过有效并行度后，调度、同步、cache coherence 和带宽竞争会让程序更慢。
- 浮点并行归约末位不同**不是** data race，是分组顺序改变了舍入。
- 不死锁不代表每个请求都能完成——livelock 和 starvation 让系统忙碌却无个体进展。

**🔧 工程落点**：加速比 $S_p = T_1/T_p$、效率 $E_p = S_p/p$，强扩展（固定总量加核）和弱扩展（每核工作量固定）回答的是不同问题，不能混算；测量必须绑核 + 多次运行 + 关闭频率波动。生产架构的常见形态是 **Reactor（I/O）+ 有界 worker pool（业务）+ MPSC completion queue + eventfd 回投**。

> **本章缺口**：§12.8 已由本文档收口，无需单独整理。

---

## 四条纵向主线（跨章串讲）

### 主线一：性能问题的定位顺序

```
现象：程序慢
  │
  ├─ 先测 α（Amdahl）：热点占比多少？ ────────────── 第 1、5 章
  │      perf record -g → perf report
  │
  ├─ 算法层：复杂度对不对？ ─────────────────────── 第 5 章（收益最大）
  │
  ├─ 访存层：cache miss 还是延迟串行？ ──────────── 第 6 章
  │      随机访问测延迟阶梯（顺序访问被预取器掩盖）
  │      行优先 / 分块 / alignas 隔离 false sharing
  │
  ├─ 指令层：延迟界限还是吞吐量界限？ ───────────── 第 5 章
  │      多累加器 / 重结合打断依赖链
  │
  ├─ 分支层：branch-misses 高不高？ ─────────────── 第 5 章
  │      只优化数据相关、模式随机的分支
  │
  └─ 系统层：缺页？上下文切换？D 状态？ ────────── 第 8、9 章
         /proc/<pid>/stat 的 minflt/majflt、vmstat、iostat
```

### 主线二：一个地址的完整旅程（第 3 + 6 + 9 章）

`&x` 在汇编里是 `leaq`（不访存）→ 得到虚拟地址 → MMU 查 TLB / 多级页表 → 物理地址 → L1/L2/L3 → DRAM。
**保护在翻译阶段完成**（MMU 查 PTE 许可位），所以 cache 用物理地址寻址且不需要做权限检查。

### 主线三：控制流突变的四个层次（第 8 + 12 章）

| 层次 | 谁触发 | 谁处理 | 典型坑 |
|------|--------|--------|--------|
| 硬件异常 | CPU/设备 | 内核异常表 | 故障返回**当前指令**重试 |
| 进程调度 | 定时器中断 | 内核调度器 | 抢占对被抢者完全透明 |
| 信号 | 内核/其他进程 | 用户 handler | 不排队、非异步信号安全函数 |
| 线程调度 | 同上 | 同上 | 共享地址空间 → 一切同步问题 |

### 主线四：边界与所有权（第 10 + 11 + 12 章）

三个"没有边界"的东西，都要应用层自己补协议：

- **字节流没有消息边界**：TCP 粘包、UDS `SOCK_STREAM`、管道 → 定长 / 长度前缀 / 分隔符
- **二进制文件没有行边界**：`0x0a` 只是数据 → 按字节数读
- **共享内存没有所有权**：地址可达不等于可以随便改 → identity/ownership/lifetime/invariant/ordering 五要素

---

## 一张延迟速查表（量级参考，本机实测值已标注）

| 操作 | 周期 | 换算（3 GHz） |
|------|------|---------------|
| 寄存器访问 | 0–1 | — |
| L1 cache 命中 | **≈4**（实测） | ~1.3 ns |
| L2 cache 命中 | **≈14**（实测） | ~4.7 ns |
| L3 cache 命中 | **≈40**（实测） | ~13 ns |
| DRAM 访问 | **≈300+**（实测） | ~100 ns |
| store→load 转发（别名） | ≈6 | ~2 ns |
| 分支预测失败 | ~15–20 | ~6 ns |
| minor fault（修 PTE） | ~10³ | ~μs 级 |
| 上下文切换 | ~10³–10⁴ | μs 级 |
| major fault（SSD 换入） | ~10⁵–10⁶ | ~0.1 ms |
| 磁盘寻道（HDD） | ~10⁷ | ~10 ms |

记住相邻两级的**数量级差**比记住具体数字重要：寄存器 → L1 → DRAM → SSD → HDD，每一跳大致是一个到两个数量级。

---

## 验收自测：七个问题的标准答案

**1. 一个 C 程序从源码到运行经历哪些阶段？**
预处理（宏展开）→ 编译（`.s`）→ 汇编（`.o`，含 `.symtab` 和重定位条目）→ 链接（符号解析 + 重定位，产出 ELF 可执行文件）→ `fork`+`execve`（删旧区域、建新 `vm_area_struct` 映射，**不读内容**）→ 跳到入口后靠 demand paging 逐页换入 → 动态链接器解析 `.so` 并通过 GOT/PLT 绑定。

**2. 为什么某些整数比较会出错？**
混合运算时有符号一律转无符号（`-1 < 0U` 为 false）；有符号溢出是 UB，编译器会删掉 `x+1 > x` 形式的检查；`unsigned` 倒序循环 `i >= 0` 恒真；截断直接丢高位连符号位一起丢。

**3. 一个函数调用时栈和寄存器发生了什么？**
`call` 把返回地址压栈并跳转；前 6 个整型参数走 `%rdi,%rsi,%rdx,%rcx,%r8,%r9`，第 7 个起逆序压栈；被调者若要用 callee-saved 寄存器（`%rbx,%rbp,%r12-15`）必须先 push；局部变量优先寄存器，放不下或需要取地址才 `subq $N,%rsp` 划出栈空间；返回值在 `%rax`；`ret` 弹返回地址。叶子函数可能完全不碰 `%rsp`。

**4. 为什么程序会出现段错误？**
访存触发缺页异常，内核缺页处理程序过三关：① 地址落在某个 `vm_area_struct` 里吗 → 不在就 SIGSEGV；② 操作符合该区域权限吗（写只读页、执行 NX 页）→ 不符就 SIGSEGV；③ 都通过才 demand paging 换入并重新执行该指令。**段错误和正常缺页是同一个入口、不同出口**。

**5. 为什么逻辑相同的两段代码性能差很多？**
访存模式不同导致 cache miss 率不同。C 数组行优先，内层循环走最右下标才是 stride-1（miss 率 ≈ 元素大小/line 大小）；走错维度每次跨一整行，几乎必 miss。计算量完全相同，性能可差几倍。此外还有依赖链长度（延迟界限）、别名阻止优化、false sharing 三个常见原因。

**6. `perf stat` 输出怎么读？**
`instructions/cycles` = IPC，低于 1 通常是停顿严重；`cache-misses / cache-references` 看访存效率；`branch-misses` 占 branches 超过 ~5% 说明分支不可预测。注意三个陷阱：混合架构要用 `cpu_core/.../` 前缀并 `taskset` 绑核；顺序访问的 miss 被预取器掩盖；括号里的覆盖率过低的行是外推噪声。

**7. 为什么会有竞态和死锁，false sharing 是什么？**
竞态的根因不是"切换太巧"，而是程序允许错误轨迹存在——共享可变状态没有建立互斥或 happens-before。死锁需要四个必要条件（互斥、持有并等待、不可抢占、循环等待），工程上靠**统一全局锁顺序 + 避免锁内 callback**规避。false sharing 是两个逻辑独立的变量落在同一条 64 字节 cache line 上，一方写就让另一方的缓存行失效——**结果正确，纯性能问题**，用 `alignas(64)` 或 padding 隔开。

---

## 完成状态

**全书正文已无缺口。** 第 1、2、3、5、6、7、8、9、10、11、12 章全部读完，各章小结由本文档收口。

| 位置 | 内容 | 状态 |
|------|------|------|
| 第 4 章 | 处理器体系结构 | ⏭️ 按计划跳过，不记录笔记（执行模型由 §5.7 从性能视角补齐） |
| 零页别名 | blocked 版每迭代 ~19 cycles 的微架构级定量归因（疑 4K aliasing / 存储转发伪依赖） | 🟡 机理已解，定量待用 §5.7 topdown + perf record 收尾 |
